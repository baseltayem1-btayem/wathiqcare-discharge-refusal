DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=wathiqcare

# Prisma / Neon PostgreSQL
DATABASE_URL="postgresql://user:password@host/db?sslmode=require&channel_binding=require"

# Backend auth/JWT signing (must match token issuer)
JWT_SECRET_KEY="replace-with-strong-secret"
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Frontend -> backend integration (Next rewrites and server-side proxy helpers)
BACKEND_URL="https://api.example.com"
BACKEND_API_BASE_URL="https://api.example.com"

# Optional for browser-side API base if not using same-origin paths
NEXT_PUBLIC_API_BASE_URL=""

# Platform production module switch
PRODUCTION_SAAS_UPGRADE_ENABLED=true

# Signature provider keys (required for platform signature sessions)
SMS_PROVIDER_API_KEY=""
SMS_PROVIDER_SENDER_ID=""
NAFATH_CLIENT_ID=""
NAFATH_CLIENT_SECRET=""
