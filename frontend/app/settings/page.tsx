import PlaceholderWorkspace from "@/ui/components/PlaceholderWorkspace";

export default function SettingsPage() {
  return <PlaceholderWorkspace title="Settings" description="Tenant and user settings UI shell is available." />;
}
