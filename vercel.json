{
  "buildCommand": "npm run runtime:pre-vercel && npm run prisma:generate && node scripts/run-sql-migrations.cjs && next build --webpack && node scripts/write-deterministic-routes-manifest.cjs",
  "installCommand": "npm install && npm run prisma:generate",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "env": {
    "NEXT_PUBLIC_ENABLE_WORKFLOW_GUIDANCE": "true"
  }
}
