# Incident Response Helpers (Archived)

These scripts were used during P0/P1 production stabilization and are intentionally archived.

- `tmp_gen_token.cjs`: Generates a short-lived JWT for controlled production validation.
- `tmp_p0_validation.cjs`: Executes end-to-end production acceptance checks used during incident recovery.

They are not part of normal runtime paths.