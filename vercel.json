{
  "buildCommand": "npm run runtime:pre-vercel && npm run prisma:generate && npm run build",
  "installCommand": "npm install && npm run prisma:generate",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "env": {
    "NEXT_PUBLIC_ENABLE_WORKFLOW_GUIDANCE": "true"
  }
}
