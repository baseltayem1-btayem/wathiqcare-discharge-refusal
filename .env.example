# ─────────────────────────────────────────────────────────────────────
# WathiqCare — .env.example
# Copy to .env (root), apps/api/.env, and apps/web/.env.local as needed.
# NEVER commit real secrets.
# ─────────────────────────────────────────────────────────────────────

# ── App URLs ──────────────────────────────────────────────────────────
APP_BASE_URL=https://wathiqcare.com
API_BASE_URL=https://api.wathiqcare.com
NEXT_PUBLIC_API_BASE_URL=https://api.wathiqcare.com

# ── Database ──────────────────────────────────────────────────────────
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=wathiqcare
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/wathiqcare"

# ── Redis / Queue ─────────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379/0

# ── Object Storage (S3 or S3-compatible) ─────────────────────────────
S3_ENDPOINT=https://s3.amazonaws.com
S3_REGION=me-south-1
S3_BUCKET=wathiqcare-documents
S3_ACCESS_KEY=your_access_key
S3_SECRET_KEY=your_secret_key

# ── SMS ───────────────────────────────────────────────────────────────
# Provider: taqnyat | unifonic | mock
SMS_PROVIDER=mock
TAQNYAT_API_KEY=
UNIFONIC_API_KEY=
SMS_SENDER_ID=WathiqCare

# ── Email (Microsoft Graph) ───────────────────────────────────────────
MICROSOFT_TENANT_ID="08b4493f-d1e2-4c61-b46f-d652ad477fa6"
MICROSOFT_CLIENT_ID="d25f4d4d-51bf-4be8-b4fd-ce8744434eef"
MICROSOFT_CLIENT_SECRET="replace-with-microsoft-graph-client-secret"
MICROSOFT_SENDER_EMAIL="admin@wathiqcare.med.sa"

# ── Payments ──────────────────────────────────────────────────────────
# Provider: hyperpay | stripe | mock
PAYMENT_PROVIDER=mock
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
HYPERPAY_ACCESS_TOKEN=
HYPERPAY_ENTITY_ID=

# ── Security ──────────────────────────────────────────────────────────
JWT_SECRET_KEY="replace-with-strong-secret"
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
# Pepper appended to raw token before sha256 hashing — rotate on full migration only
PUBLIC_LINK_TOKEN_PEPPER=change_me_pepper_secret
OTP_SECRET=change_me_otp_secret

# ── Backend proxy (Next.js server-side) ──────────────────────────────
BACKEND_URL="http://localhost:8000"
BACKEND_API_BASE_URL="http://localhost:8000"
